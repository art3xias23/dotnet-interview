**Key Concepts**

- Uses hash table internally.
    
- Hash codes map keys to buckets.
    
- Collisions handled with linked lists or similar chaining.
    

**Collision Handling**

- Different keys can have same hash code → stored in the same bucket + resolved by chaining.