**Eager Loading**

- Loads related data up front in the query.
    
- Avoids extra DB calls but increases initial memory usage.
    

**Lazy Loading**

- Loads related data only when accessed.
    
- Uses separate queries, lower upfront memory cost.